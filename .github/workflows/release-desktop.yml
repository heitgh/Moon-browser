name: Desktop Releases

on:
  pull_request:
    paths:
      - .github/workflows/release-desktop.yml
      - electron-builder.yml
      - package.json
      - package-lock.json
      - scripts/build/**
      - apps/desktop/**
      - packages/**
      - ui/**
      - assets/**
      - index.html
      - main.js
      - preload.cjs
      - tsconfig*.json
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - .github/release-trigger
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag da release (ex.: v0.6.0-alpha.1)"
        required: true
        default: "v0.6.0-alpha.2"
        type: string
      publish_release:
        description: "Publicar os instaladores em GitHub Releases"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: desktop-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  quality:
    uses: ./.github/workflows/quality.yml

  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x64
            os: windows-latest
            artifact: moon-browser-windows-x64
          - name: Linux x64
            os: ubuntu-latest
            artifact: moon-browser-linux-x64

    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Linux packaging toolchain
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install --no-install-recommends -y rpm libarchive-tools

      - name: Typecheck
        run: npm run typecheck

      - name: Rebuild native dependencies for Electron
        run: npm run native:electron

      - name: Build installers
        run: npm run build:desktop

      - name: Create Windows checksums
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $files = Get-ChildItem release -File -Filter *.exe
          $lines = $files | ForEach-Object {
            $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower()
            "$hash  $($_.Name)"
          }
          $lines | Set-Content release/SHA256SUMS-windows.txt

      - name: Create Linux checksums
        if: runner.os == 'Linux'
        shell: bash
        run: |
          cd release
          shopt -s nullglob
          files=( *.AppImage *.deb *.rpm *.pacman )
          if (( ${#files[@]} == 0 )); then
            echo "Nenhum pacote Linux foi gerado." >&2
            exit 1
          fi
          sha256sum "${files[@]}" > SHA256SUMS-linux.txt

      - name: Upload Windows installers
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            release/*.exe
            release/SHA256SUMS-windows.txt
          if-no-files-found: error
          retention-days: 14

      - name: Upload Linux packages
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            release/*.AppImage
            release/*.deb
            release/*.rpm
            release/*.pacman
            release/SHA256SUMS-linux.txt
          if-no-files-found: error
          retention-days: 14

  release:
    name: Publish GitHub Release
    environment: release-approval
    needs: [build, quality]
    if: >-
      (github.event_name == 'workflow_dispatch' && inputs.publish_release) ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout release source
        uses: actions/checkout@v6

      - name: Resolve release tag
        id: release
        shell: bash
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          if [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]]; then
            tag="$INPUT_TAG"
          elif [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            tag="$GITHUB_REF_NAME"
          else
            tag=v$(node -p "require('./package.json').version")
          fi
          expected="v$(node -p "require('./package.json').version")"
          if [[ "$tag" != "$expected" ]]; then
            echo "A tag precisa corresponder à versão compilada: $expected" >&2
            exit 1
          fi
          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            trigger="$(grep -E '^v[0-9]' .github/release-trigger | head -n 1)"
            if [[ "$trigger" != "$expected" ]]; then
              echo "O release-trigger precisa corresponder à versão compilada: $expected" >&2
              exit 1
            fi
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Download installers
        uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true

      - name: Publish release and direct-download assets
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
        shell: bash
        run: |
          notes_file="docs/releases/${RELEASE_TAG}.md"
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release já existe. Use uma nova versão; artefatos publicados são imutáveis." >&2
            exit 1
          else
            if [[ -f "$notes_file" ]]; then
              gh release create "$RELEASE_TAG" release/* \
                --repo "$GITHUB_REPOSITORY" \
                --target "$GITHUB_SHA" \
                --title "Moon Browser $RELEASE_TAG" \
                --notes-file "$notes_file" \
                --prerelease
            else
              gh release create "$RELEASE_TAG" release/* \
                --repo "$GITHUB_REPOSITORY" \
                --target "$GITHUB_SHA" \
                --title "Moon Browser $RELEASE_TAG" \
                --generate-notes \
                --prerelease
            fi
          fi
